# Enhanced Unit Converter CLI

Convert between multiple unit types with improved error handling and support for additional categories.

## Usage
```bash
convert.py 100 --from-unit m --to-unit ft --category length
```

## Features
- Supports length, weight, and temperature conversions
- Automatic unit validation
- Clear error messages
- Command-line interface